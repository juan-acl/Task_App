import { Dispatch } from "redux";
import { API } from "../../helpers/API";
import { ActivityIndicator } from "react-native";


export const getMyTask = (id: Number) => async (dispatch: Dispatch ) => {
    try{

    }catch(error){
        console.log('Error al obtener mis tareas', error)
    }finally{

    }
}